# Install only when using local embeddings.
# This file is intentionally separate so base Docker builds stay fast.
# CPU-only torch avoids downloading large CUDA dependency stacks.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu ; platform_system == "Linux"
sentence-transformers>=2.2.0
